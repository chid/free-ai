# CLAUDE.md

**Read [`AGENTS.md`](AGENTS.md) — it is the project guide and the source of truth for
how to work in this repo.** Everything that used to live here now lives there.

This repo follows the cross-tool `AGENTS.md` convention so that every agent working in
it — Claude Code, Antigravity, Cursor, Codex — reads one set of conventions rather than
several that drift apart. This file exists only because Claude Code auto-loads
`CLAUDE.md`; it deliberately holds no instructions of its own.

Do not add project instructions here. Add them to `AGENTS.md`.
